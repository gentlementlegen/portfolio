overwrite: true
schema: http://${API_URL}/api/graphql
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
  src/generated/schema.graphql:
    plugins:
      - "schema-ast"
config:
  namingConvention:
    typeNames: change-case-all#pascalCase
    transformUnderscore: true